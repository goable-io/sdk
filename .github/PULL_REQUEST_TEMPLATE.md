## What this PR changes

(brief description)

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes (includes the generated-types freshness guard)
- [ ] `pnpm build` passes
- [ ] If this touches `openapi.json`, I ran `pnpm gen` and committed the
      updated `src/generated/api.ts` (see CONTRIBUTING.md → "Keeping the
      contract in sync")
